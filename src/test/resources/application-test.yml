spring:
  datasource:
    url: jdbc:h2:mem:trackpro;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

trackpro:
  bootstrap:
    admin:
      enabled: true
      email: admin@trackpro.local
      password: admin123!
      display-name: Admin
  security:
    jwt:
      issuer: trackpro
      secret: dev-change-me-dev-change-me-dev-change-me-dev-change-me
      access-token-ttl: PT15M
      refresh-token-ttl: P7D
